<?xml version="1.0" encoding="utf-8"?>
<!--
  productbuild distribution definition.

  @@VERSION@@, @@PKG_ID@@ and @@HOST_ARCH@@ are substituted by build-pkg.sh so
  the version and target architecture stay single-sourced.
-->
<installer-gui-script minSpecVersion="2">
    <title>THU Auto Login</title>
    <organization>com.thu.autologin</organization>

    <!-- System-wide install: that is what makes the unpacked extension land on
         a fixed path, and therefore what gives every user the same extension ID
         and the same native messaging manifest. -->
    <domains enable_localSystem="true" enable_anywhere="false" enable_currentUserHome="false"/>

    <options customize="never" require-scripts="false" hostArchitectures="@@HOST_ARCH@@"/>

    <welcome file="welcome.html" mime-type="text/html"/>
    <conclusion file="conclusion.html" mime-type="text/html"/>

    <choices-outline>
        <line choice="default">
            <line choice="@@PKG_ID@@"/>
        </line>
    </choices-outline>

    <choice id="default"/>
    <choice id="@@PKG_ID@@" visible="false">
        <pkg-ref id="@@PKG_ID@@"/>
    </choice>

    <pkg-ref id="@@PKG_ID@@" version="@@VERSION@@" onConclusion="none">component.pkg</pkg-ref>
</installer-gui-script>
